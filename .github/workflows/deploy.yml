name: Build Tarhan website

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: tarhan-build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build website
        run: npm run build

      - name: Upload production build
        uses: actions/upload-artifact@v4
        with:
          name: tarhan-dist
          path: dist
          if-no-files-found: error
